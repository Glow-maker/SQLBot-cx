# Token中间件 Redis 用户恢复改造说明

本文档说明本次 SQLBot Token 改造的设计与实现，目标是让 `chat` 模块支持直接使用数据中台（Yudao）Redis 中的 `access_token` 恢复用户上下文，并设置到 `request.state.current_user`。

## 1. 改造背景

SQLBot 原有认证逻辑是：

1. 请求头获取 Bearer token（`X-SQLBOT-TOKEN`）
2. 按 SQLBot 本地 `SECRET_KEY` 解 JWT
3. 根据 JWT 中 `id` 到 SQLBot 本地数据库查询用户
4. 设置 `request.state.current_user`

该逻辑不兼容数据中台 OAuth2 随机 token（非 JWT）。  
数据中台的 token 信息保存在 Redis，key 形如：

`oauth2_access_token:{token}`

因此需要新增一条认证路径：

1. 从请求头拿 token
2. 到 Redis 读取 token 详情 JSON
3. 从 JSON 中提取 `userId / userInfo.username / tenantId`
4. 构建 SQLBot `UserInfoDTO`
5. 写入 `request.state.current_user`

## 2. 本次改造范围

本次只对 `chat` 路由启用 Redis token 恢复逻辑，路径前缀：

`{API_V1_STR}/chat`

其余模块保持原有 JWT 逻辑不变，避免全局行为变化。

## 3. 修改文件清单

### 3.1 新增文件

1. `backend/common/core/token_resolver.py`

### 3.2 修改文件

1. `backend/apps/system/middleware/auth.py`
2. `backend/common/core/config.py`
3. `backend/common/core/schemas.py`

### 3.3 备份文件（改造前）

目录：

`backup/token_middleware_redis_20260302_005141`

文件：

1. `backup/token_middleware_redis_20260302_005141/auth.py.bak`
2. `backup/token_middleware_redis_20260302_005141/config.py.bak`
3. `backup/token_middleware_redis_20260302_005141/schemas.py.bak`

## 4. 核心流程（运行时）

```mermaid
sequenceDiagram
    participant Client as 前端/调用方
    participant MW as TokenMiddleware
    participant Resolver as token_resolver
    participant Redis as 中台Redis
    participant Chat as Chat接口

    Client->>MW: Authorization: Bearer <token>
    MW->>MW: validateToken(token, request_path)
    alt chat 路由
        MW->>Resolver: resolve_platform_token_identity(token)
        Resolver->>Redis: GET oauth2_access_token:{token}
        Redis-->>Resolver: token JSON
        Resolver-->>MW: PlatformTokenIdentity
        MW->>MW: _build_user_from_platform_token(...)
        MW->>MW: request.state.current_user = UserInfoDTO
        MW->>Chat: call_next(request)
    else 非 chat 路由
        MW->>MW: 原本地JWT校验 + DB查用户
    end
```

## 5. 文件级详细说明

## 5.1 `backend/common/core/token_resolver.py`

职责：只做“中台 Redis token 解析”，不做 SQLBot 本地 JWT。

### 关键数据结构

`PlatformTokenIdentity`：

1. `user_id`: 来自 `token_record.userId`
2. `account`: 来自 `token_record.userInfo.username`（可配置字段）
3. `tenant_id`: 来自 `token_record.tenantId`
4. `nickname`: 来自 `token_record.userInfo.nickname`
5. `payload`: 原始 token 记录，保留扩展能力

### 关键函数

1. `_normalize_redis_key(access_token)`
说明：根据配置拼出 Redis key。默认 `oauth2_access_token:%s`。

2. `_get_platform_auth_redis_client()`
说明：按 `PLATFORM_AUTH_REDIS_URL` 获取异步 Redis 客户端，单例复用。

3. `_build_platform_identity(token_record)`
说明：
   - 校验过期时间 `expiresTime`
   - 可选校验 `userType`
   - 提取 `userId/account/tenantId/nickname`
   - 字段缺失则返回 `None`

4. `resolve_platform_token_identity(access_token)`
说明：对外入口。读取 Redis 并返回 `PlatformTokenIdentity` 或 `None`。

### 解析规则

1. `userInfo` 支持对象或 JSON 字符串
2. 账号字段优先读取配置 `PLATFORM_AUTH_USERINFO_ACCOUNT_FIELD`，再回退 `username/account`
3. `tenantId` 缺失时尝试回退 `oid`

## 5.2 `backend/apps/system/middleware/auth.py`

职责：认证中间件入口，最终决定 `request.state.current_user`。

### 关键改动点

1. `dispatch()`
   - 增加 `Authorization` 回退读取
   - 调用 `validateToken(token, trans, request.url.path)`

2. `validateToken(...)`
   - 当请求是 `chat` 路由时，优先调用 `resolve_platform_token_identity(param)`
   - Redis 命中时直接构造 `UserInfoDTO` 返回
   - Redis 未命中则继续原 JWT 逻辑

3. `_is_chat_request(request_path)`
   - 判断是否属于 `{API_V1_STR}/chat` 路径

4. `_build_user_from_platform_token(identity)`
   - 将 `PlatformTokenIdentity` 映射为 `UserInfoDTO`
   - 填充 chat 需要字段并给非关键字段默认值

### 字段映射

| 中台 token 字段 | SQLBot UserInfoDTO 字段 |
| --- | --- |
| `user_id` | `id` |
| `account` | `account` |
| `tenant_id` | `oid` |
| `nickname` | `name` |

默认补齐：

1. `email = {account}@platform.local`
2. `status = 1`
3. `origin = 1`
4. `language = zh-CN`
5. `weight = 0`
6. `isAdmin = False`

## 5.3 `backend/common/core/config.py`

新增配置项：

1. `PLATFORM_AUTH_ENABLED`
2. `PLATFORM_AUTH_REDIS_URL`
3. `PLATFORM_AUTH_REDIS_KEY_PREFIX`
4. `PLATFORM_AUTH_USERINFO_ACCOUNT_FIELD`
5. `PLATFORM_AUTH_REQUIRE_USER_TYPE`
6. `PLATFORM_AUTH_ACCEPT_AUTHORIZATION_HEADER`

并将布尔配置加入 `field_validator` 自动转换列表。

## 5.4 `backend/common/core/schemas.py`

`XOAuth2PasswordBearer.__call__` 增加：

当 `X-SQLBOT-TOKEN` 不存在且开关开启时，回退读取 `Authorization` 头。

## 6. 行为矩阵

| 场景 | 结果 |
| --- | --- |
| chat 路由 + Redis 命中中台 token | 通过；使用 Redis 恢复 `current_user` |
| chat 路由 + Redis 未命中 | 回退本地 JWT 校验 |
| 非 chat 路由 + 中台随机 token | 按本地 JWT 校验，通常会失败 |
| 非 chat 路由 + SQLBot 本地 JWT | 原逻辑通过 |

## 7. 配置示例

## 7.1 开发阶段（SSH 隧道）

```env
PLATFORM_AUTH_ENABLED=true
PLATFORM_AUTH_REDIS_URL=redis://127.0.0.1:16379/1
PLATFORM_AUTH_REDIS_KEY_PREFIX=oauth2_access_token:%s
PLATFORM_AUTH_USERINFO_ACCOUNT_FIELD=username
PLATFORM_AUTH_ACCEPT_AUTHORIZATION_HEADER=true
```

## 7.2 部署阶段（内网直连）

```env
PLATFORM_AUTH_ENABLED=true
PLATFORM_AUTH_REDIS_URL=redis://:<password>@<redis-ip>:6379/1
PLATFORM_AUTH_REDIS_KEY_PREFIX=oauth2_access_token:%s
PLATFORM_AUTH_USERINFO_ACCOUNT_FIELD=username
PLATFORM_AUTH_ACCEPT_AUTHORIZATION_HEADER=true
```

可选：

```env
PLATFORM_AUTH_REQUIRE_USER_TYPE=2
```

用于限制仅接受指定用户类型。

## 8. 联调验证步骤

1. Redis 连通：
```bash
redis-cli -h <redis-host> -p <port> -n 1 PING
```

2. 检查 token key：
```bash
redis-cli -h <redis-host> -p <port> -n 1 --scan --pattern 'oauth2_access_token:*' | head
```

3. 抽样读取 token：
```bash
redis-cli -h <redis-host> -p <port> -n 1 GET 'oauth2_access_token:<token>'
```

4. 调用 chat 接口：
```bash
curl -H "Authorization: Bearer <token>" http://<sqlbot-host>/api/v1/chat/list
```

5. 预期：
   - chat 请求可通过
   - `request.state.current_user` 为 Redis 恢复结果

## 9. 常见问题

1. Redis 有 key 但认证失败
原因：
   - key 在错误 db（0/1 混用）
   - `PLATFORM_AUTH_REDIS_KEY_PREFIX` 不一致
   - token 过期
   - `userId/userInfo.username/tenantId` 缺失

2. 非 chat 接口用中台 token 失败
原因：
   - 当前设计只对 chat 启用 Redis 恢复
   - 非 chat 仍按本地 JWT 校验

3. `Authorization` 头未生效
排查：
   - `PLATFORM_AUTH_ACCEPT_AUTHORIZATION_HEADER=true`
   - 实际请求是否带了 `Bearer` 方案

## 10. 后续扩展建议

1. 若要全站支持中台 token：
   - 放宽 `_is_chat_request` 限制
   - 评估 `system` 模块对本地用户字段的依赖（如 `weight/isAdmin/system_variables`）

2. 增加调试日志：
   - Redis 命中/未命中
   - 用户字段映射结果（脱敏）

3. 增加回退策略：
   - Redis 未命中时，可选调用中台 token 校验 API

4. 增加集成测试：
   - chat + Redis token 命中
   - chat + Redis token 过期
   - non-chat + 本地 JWT

